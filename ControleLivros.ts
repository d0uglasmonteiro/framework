import "Livro.ts"
let livros:string[]=["01","02","03"]

class ControleLivro
{private obterLivros:=>{livros}
private incluir:type livro={codigo:number}{codigo=livros.filter(index=2)++1;livros.push(codigo)}
private excluir:number={findIndex(testfn(excluir[index[livros]]))}}



