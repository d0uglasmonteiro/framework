import {controleEditora} from 'index.ts'
export default (req:NextApiRequest, res:NextApiResponse)=>{GET:200; getNomeEditora;}
import {controleLivro}from 'index;ts
export default(req: NextApiRequest, res:NextApiResponse)=>{}'