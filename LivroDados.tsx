import type {NextPage} from 'next'
import React from 'react';
const LivroDados: NextPage=()=>{return (<main>Ola Mundo</main>)}
import styles from '../styles/Home.module.css'
controleEditora:
const baseURL='http://localhost:3000/api/livros'
incluirLivro(livro)=>{fetch(baseURL)}
opcoes:[]=[getEditoras;codEditora:value;nome:text;]
titulo=useState(text)
resumo=useState(text)
autores=useState(text)
codEditora=opcoes(index:0)
navigate=useNavigate
tratarCombo(React.ChageEvent<HTMLSelectElement>)=>{setCodEditora}
incluir(React.FormEvent<HTMLFormElement>)=>{preventDefault;livro=0;autores;incluir{return Router.push('LivroLista');
<div style=(styles.container)>Head,Menu,<main>Titulo: <form ng-submit="incluir"><label for='titulo'>Titulo</label><br><input type="text"><br></input>
<label for='resumo'>Resumo</label><br><input type="text"><br></input>
<label for='editora'>Editora</label><br><input type="text"><br></input>
<label for='autores'>Autores</label><br><input type="text"></input>
</form><button>Salvar Dados</button></main>}}
